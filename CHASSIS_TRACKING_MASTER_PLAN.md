# 小车底盘、循迹、里程计与实时架构总控文档

> 项目仓库：`Jhon0213/bupt-2026-e-car`  
> 默认分支：`main`  
> 文档定位：底盘与循迹子系统的长期总控文档  
> 使用方式：每次开始分析、调试、修改代码前先阅读本文件；每完成一个阶段后更新状态、实验结果和下一步动作。

---

## 1. 文档目标

本文件用于统一管理以下四条强耦合主线：

1. 直线循迹时车辆左右摇摆问题；
2. 半圆弯道的连续、稳定、丝滑转向问题；
3. 编码器里程计的精度、稳定性与终点判定问题；
4. 控制任务周期、程序阻塞和代码结构重构问题。

这些问题不能完全独立处理。实际依赖关系如下：

```text
任务调度与真实控制周期
        ↓
编码器采样、测速和控制器 dt 是否可信
        ↓
左右轮速度控制是否一致
        ↓
直线循迹与弯道循迹是否稳定
        ↓
里程累计和路段识别是否可靠
        ↓
终点检测、减速和停车是否准确
```

因此，处理局部问题时必须同步检查它对整体控制链路的影响。

---

## 2. 赛题约束

根据 H 题要求，赛道和车辆运动相关约束如下：

- 赛道由两段长度为 `1.5 m` 的直线和两个半径为 `0.5 m` 的半圆组成；
- 理论中心线总长度约为：

```text
L = 2 × 1.5 + 2 × π × 0.5
  = 3 + π
  ≈ 6.1416 m
```

- 小车需要沿黑色环形路线顺时针行驶；
- 一圈后需要回到 A 点；
- 基础任务要求总时间不超过 `20 s`；
- 停车偏差不超过 `2 cm`；
- A 点具有一条与环路垂直、长度约 `5 cm` 的黑色启停线；
- 小车循迹只能使用红外光电模块；
- 行驶过程中车体投影不得完全脱离轨迹线。

工程含义：

1. 里程计必须足以完成路段定位和终点搜索窗口判断；
2. 最终厘米级停车不能只依赖累计里程；
3. A 点启停线应作为最终绝对位置基准；
4. 入弯、稳态弯道、出弯不能使用突变式控制；
5. 所有控制算法必须建立在稳定、可测量的任务周期之上。

---

## 3. 当前总体目标

### 3.1 功能目标

- 直线段无明显周期性左右摇摆；
- 半圆弯道连续转向，不出现急打方向、折线式转弯或出弯过冲；
- 一圈里程误差显著降低，能够稳定用于路段状态判断；
- 最终停车综合误差满足 `≤ 2 cm`；
- 所有关键控制任务按照设计周期稳定执行；
- OLED、通信、日志和其他非关键任务不得阻塞控制任务；
- 代码结构便于后续接入滚球平衡控制，不造成调度冲突。

### 3.2 工程目标

- 每个结论都有代码依据或实验数据依据；
- 每次参数修改均可回退；
- 每轮实车实验记录参数、环境、波形和结果；
- 不在问题来源尚未确认前进行大规模重构；
- 不使用“感觉更好”替代可量化测试。

---

## 4. 当前控制系统抽象

建议始终使用以下抽象理解系统：

```text
灰度传感器
    ↓
线位置估计 / 偏差计算
    ↓
循迹控制器
    ↓
基础速度 + 差速修正
    ↓
左、右轮目标速度
    ↓
左右速度 PI
    ↓
左右电机 PWM
    ↓
车辆运动
```

编码器反馈链路：

```text
左右编码器计数
    ↓
增量脉冲
    ↓
左右轮速度估计
    ↓
速度 PI 反馈
```

里程计链路：

```text
左右编码器累计增量
    ↓
左右轮距离
    ↓
车体中心距离 / 航向变化
    ↓
累计里程
    ↓
路段状态机
    ↓
终点搜索窗口
```

终点判定链路：

```text
已完成规定路段
AND
累计里程进入终点窗口
AND
检测到 A 点横向启停线
AND
检测结果连续稳定
        ↓
进入制动状态
        ↓
停车位置补偿
```

---

## 5. 总体工作原则

### 5.1 先测量，再修改

禁止仅凭代码阅读直接判断任务周期稳定。必须测量：

- 实际任务周期；
- 最小、平均、最大周期；
- 超周期次数；
- 各模块执行时间；
- 中断占用；
- OLED、串口、星闪是否存在阻塞；
- 主循环是否存在长时间等待。

### 5.2 先处理基础链路，再调循迹参数

推荐顺序：

```text
实时性测量
→ 编码器计数确认
→ 速度估计确认
→ 左右速度环一致性
→ 里程标定
→ 直线循迹
→ 半圆弯道
→ 终点停车
→ 渐进式重构
```

### 5.3 里程计不是最终停车基准

里程计适合回答：

- 当前大致位于哪一段；
- 是否接近终点；
- 是否应该开始搜索启停线；
- 是否完成了一圈。

启停线适合回答：

- A 点准确位置在哪里；
- 应在何时触发停车流程。

### 5.4 每次只解决一个主要问题

每次任务必须绑定一个问题编号，避免同时修改多个控制环节后无法判断效果来源。

---

## 6. 问题编号与状态

状态枚举：

```text
TODO        尚未开始
ANALYZING   正在分析
TESTING     正在测试
FIXING      正在修改
VERIFYING   正在复测
DONE        已完成
BLOCKED     被其他问题阻塞
```

### 6.1 实时架构

| 编号 | 问题 | 状态 | 依赖 |
|---|---|---:|---|
| ARCH-001 | 测量控制任务真实周期和抖动 | TODO | 无 |
| ARCH-002 | 测量各模块执行时间 | TODO | ARCH-001 |
| ARCH-003 | 排查阻塞调用 | TODO | ARCH-001 |
| ARCH-004 | 建立控制任务和非关键任务分频调度 | TODO | ARCH-001~003 |
| ARCH-005 | 进行渐进式代码结构重构 | TODO | 前述控制行为稳定 |

### 6.2 编码器与里程计

| 编号 | 问题 | 状态 | 依赖 |
|---|---|---:|---|
| ODOM-001 | 确认编码器计数模式和每圈脉冲数 | TODO | ARCH-001 |
| ODOM-002 | 检查计数溢出、符号和方向 | TODO | ODOM-001 |
| ODOM-003 | 确认速度计算公式与实际 dt | TODO | ARCH-001、ODOM-001 |
| ODOM-004 | 左右轮分别标定距离系数 | TODO | ODOM-001~003 |
| ODOM-005 | 直线 1 m、3 m 距离测试 | TODO | ODOM-004 |
| ODOM-006 | 半圆和整圈累计误差测试 | TODO | ODOM-005 |
| ODOM-007 | 分离比例误差、转弯侧滑误差和积分误差 | TODO | ODOM-006 |

### 6.3 直线循迹

| 编号 | 问题 | 状态 | 依赖 |
|---|---|---:|---|
| TRACK-001 | 采集直线摆动时灰度偏差和轮速波形 | TODO | ARCH-001 |
| TRACK-002 | 检查灰度位置估计是否跳变 | TODO | TRACK-001 |
| TRACK-003 | 检查循迹控制器增益和输出限幅 | TODO | TRACK-001 |
| TRACK-004 | 检查左右速度环响应一致性 | TODO | ODOM-003 |
| TRACK-005 | 检查航向环与循迹环是否互相冲突 | TODO | TRACK-001 |
| TRACK-006 | 加入必要的滤波、死区或输出变化率限制 | TODO | TRACK-002~005 |
| TRACK-007 | 完成不同速度下的直线稳定性验证 | TODO | TRACK-006 |

### 6.4 半圆弯道

| 编号 | 问题 | 状态 | 依赖 |
|---|---|---:|---|
| CURVE-001 | 记录入弯、弯中、出弯的控制量 | TODO | TRACK-007 |
| CURVE-002 | 建立直线、入弯、稳态弯道、出弯状态 | TODO | ODOM-006 |
| CURVE-003 | 建立弯道基础差速或曲率前馈 | TODO | CURVE-001 |
| CURVE-004 | 设计速度和差速斜坡过渡 | TODO | CURVE-002~003 |
| CURVE-005 | 优化弯中灰度反馈修正 | TODO | CURVE-003 |
| CURVE-006 | 完成左右两个半圆一致性验证 | TODO | CURVE-004~005 |

### 6.5 终点检测与停车

| 编号 | 问题 | 状态 | 依赖 |
|---|---|---:|---|
| STOP-001 | 建立路段状态机 | TODO | ODOM-006、CURVE-006 |
| STOP-002 | 建立终点里程搜索窗口 | TODO | STOP-001 |
| STOP-003 | 稳定识别 A 点横向启停线 | TODO | TRACK-002 |
| STOP-004 | 融合路段、里程和启停线判定 | TODO | STOP-001~003 |
| STOP-005 | 建立减速、制动和停车状态机 | TODO | STOP-004 |
| STOP-006 | 标定制动距离和停车补偿 | TODO | STOP-005 |
| STOP-007 | 验证停车误差不超过 2 cm | TODO | STOP-006 |

---

## 7. 阶段 0：冻结基线

### 7.1 操作

在进行任何进一步修改前记录当前提交：

```powershell
git rev-parse HEAD
```

建议为当前可运行版本打标签：

```powershell
git tag chassis-baseline-v1
git push origin chassis-baseline-v1
```

### 7.2 必须记录

```text
基线 commit：
测试日期：
电池电压：
小车负载：
轮胎状态：
基础速度：
循迹参数：
左右速度 PI 参数：
编码器参数：
当前一圈里程读数：
当前实际赛道长度：
当前停车误差：
```

### 7.3 完成标准

- 当前版本可随时回退；
- 基线参数和现象已记录；
- 后续实验能与基线对比。

---

## 8. 阶段 1：实时性与任务周期审计

### 8.1 目标

确认设计为 `10 ms` 的任务是否真实以 `10 ms` 运行，以及最坏情况下是否出现 `20 ms` 或更长间隔。

### 8.2 必测指标

```text
control_dt_min_us
control_dt_avg_us
control_dt_max_us
control_overrun_count

encoder_exec_us
gray_exec_us
tracking_exec_us
speed_pi_exec_us
motor_exec_us
imu_exec_us
oled_exec_us
communication_exec_us
```

### 8.3 建议测量方式

优先级从高到低：

1. GPIO 翻转配合示波器或逻辑分析仪；
2. MCU 定时器时间戳；
3. 环形缓冲区记录时间戳，低频输出；
4. 禁止在 10 ms 控制任务中直接大量串口打印。

GPIO 测量示意：

```c
DEBUG_PIN_HIGH();

Encoder_Update();
Gray_Update();
Tracking_Update();
SpeedPI_Update();
Motor_Apply();

DEBUG_PIN_LOW();
```

通过脉冲宽度测执行时间，通过相邻脉冲间隔测实际周期。

### 8.4 重点排查对象

- `delay_ms`、忙等待；
- OLED 全屏刷新；
- 串口等待发送完成；
- 星闪模块同步发送；
- 在控制周期中执行格式化字符串；
- 长数组遍历；
- 动态内存；
- 关闭中断时间过长；
- 多个任务在同一次循环集中执行；
- 控制器使用固定 `dt=0.01`，但实际周期并不稳定。

### 8.5 完成标准

建议目标：

```text
平均周期：接近 10 ms
最大周期：不超过 11~12 ms
控制任务执行时间：显著小于 10 ms
超周期次数：正常运行期间为 0
```

具体阈值应结合当前 MCU 和任务量调整，但必须保留足够裕量。

---

## 9. 阶段 2：编码器与里程计校准

### 9.1 首先确认换算链路

必须明确：

```text
编码器原始分辨率
× 计数模式（1X / 2X / 4X）
× 电机减速比
= 车轮每转对应计数
```

距离换算：

```text
wheel_distance = encoder_delta × distance_per_count
distance_per_count = effective_wheel_circumference / counts_per_wheel_rev
```

车体中心前进距离可先采用：

```text
center_distance = (left_distance + right_distance) / 2
```

### 9.2 禁止过早使用理论轮径

实际有效轮径受到以下因素影响：

- 轮胎压缩；
- 负载；
- 地面材质；
- 轮胎磨损；
- 左右轮制造差异；
- 电机和安装偏心。

因此应分别标定：

```text
K_left
K_right
```

修正形式示例：

```text
left_distance_corrected  = left_distance_raw  × K_left
right_distance_corrected = right_distance_raw × K_right
```

### 9.3 标定流程

#### 测试 A：悬空转轮

- 左右轮分别旋转固定圈数；
- 记录原始计数；
- 确认每圈脉冲数；
- 检查正反转符号；
- 检查计数溢出。

#### 测试 B：地面直行 1 m

至少重复 5 次，记录：

```text
左轮脉冲
右轮脉冲
左轮换算距离
右轮换算距离
平均距离
实际距离
误差
```

#### 测试 C：地面直行 3 m

用于验证误差是否近似按比例增长。

#### 测试 D：单个半圆

用于分析转弯时侧滑和左右轮路径差异。

#### 测试 E：完整一圈

理论中心线长度约 `6.1416 m`，但真实车辆传感器轨迹、车体中心轨迹和轮胎轨迹可能不同，因此必须说明当前测量对象是哪条轨迹。

### 9.4 误差分类

#### 比例误差

表现：

```text
距离越长，误差近似线性增长
```

可能原因：

- 每圈脉冲数错误；
- 轮径错误；
- 减速比错误；
- 计数倍频错误。

#### 周期积分误差

表现：

```text
速度值看似正常，但累计距离随任务阻塞漂移
```

可能原因：

- 使用 `speed × 固定 10 ms` 积分；
- 实际周期不是 10 ms；
- 丢失编码器增量。

优先直接累计编码器距离，而不是通过速度二次积分得到里程。

#### 转弯侧滑误差

表现：

```text
直线标定准确，完整一圈仍然误差较大
```

可能原因：

- 半圆中内外轮侧滑；
- 车辆几何参数不准确；
- 车轮轨迹与赛道中心线不一致；
- 转弯速度过高。

---

## 10. 阶段 3：直线左右摇摆定位

### 10.1 必须先采集的数据

建议以固定周期记录：

```text
timestamp
gray_raw[]
line_position
line_error
line_error_derivative
tracking_output
left_target_speed
right_target_speed
left_measured_speed
right_measured_speed
left_pwm
right_pwm
yaw
route_state
```

### 10.2 典型原因与验证方法

#### 原因 A：循迹比例增益过大

特征：

- 误差一出现，左右轮目标速度立即大幅分离；
- 车辆越过中心线后反向修正；
- 形成固定频率摆动。

验证：

- 降低基础速度；
- 将循迹 P 暂时减小；
- 比较摆动幅度是否明显下降。

#### 原因 B：灰度位置估计跳变

特征：

- 车辆运动平缓，但 `line_position` 离散跳动；
- 相邻周期误差变化过大。

验证：

- 静止放在线上采集原始灰度；
- 缓慢横向移动小车；
- 检查位置输出是否连续、单调。

#### 原因 C：D 项放大噪声

特征：

- 误差较小时控制输出仍高频抖动；
- D 项波形尖峰明显。

验证：

- 暂时关闭 D；
- 对微分项使用低通滤波；
- 使用真实 `dt`。

#### 原因 D：左右速度环不一致

特征：

- 相同目标速度下左右实测速度长期不同；
- 直线时循迹控制不断补偿机械偏差。

验证：

- 暂时关闭循迹，只给左右相同目标速度；
- 记录左右速度和 PWM；
- 分别整定左右速度环。

#### 原因 E：多个控制环互相打架

特征：

- 循迹输出向左修正，航向控制同时向右修正；
- 合成输出出现周期性反转。

验证：

- 分别关闭某个外环；
- 明确控制优先级和叠加方式。

### 10.3 推荐控制结构

```text
base_speed
+ curvature_feedforward
+ line_feedback
+ limited_heading_correction
        ↓
left_target_speed
right_target_speed
```

所有修正项必须：

- 有明确物理意义；
- 有输出限幅；
- 有必要的变化率限制；
- 不重复控制同一误差。

### 10.4 完成标准

建议在多个基础速度下测试：

```text
低速
目标比赛速度的 70%
目标比赛速度
```

每档至少重复 5 次，记录：

- 最大横向偏差；
- 摆动次数；
- 摆动衰减情况；
- 是否丢线；
- 左右轮速度误差。

---

## 11. 阶段 4：半圆丝滑转弯

### 11.1 不推荐的方式

不推荐仅在某个里程点将左右速度从直线参数瞬间切换为弯道参数：

```text
直线：left = 500, right = 500
弯道：left = 300, right = 700
```

这种阶跃会造成：

- 车身姿态突变；
- 内外轮打滑；
- 灰度误差骤增；
- 滚球控制受到冲击；
- 出弯过冲。

### 11.2 推荐路段状态

```text
STRAIGHT
ENTER_CURVE
CURVE_STEADY
EXIT_CURVE
FINAL_STRAIGHT
```

### 11.3 推荐控制方式

#### 直线段

```text
较高基础速度
较小曲率前馈
灰度反馈负责纠偏
```

#### 入弯段

```text
基础速度平滑下降
目标差速平滑增加
灰度反馈保持线路
```

#### 稳态弯道

```text
固定或缓慢变化的曲率前馈
灰度反馈只做残差修正
```

#### 出弯段

```text
目标差速平滑减小
基础速度平滑恢复
防止反向过度纠偏
```

### 11.4 平滑过渡

可采用：

- 线性斜坡；
- 一阶低通；
- S 曲线；
- 每周期最大变化量限制。

示例：

```text
target_diff = target_diff_prev
            + clamp(desired_diff - target_diff_prev,
                    -diff_step_max,
                    +diff_step_max)
```

### 11.5 曲率前馈

对于差速小车，理想几何关系可作为初始参考，但最终需要实车标定。

核心思想：

```text
弯道所需基础差速由赛道曲率提前给出
灰度控制器只纠正实际偏差
```

这样可以避免完全依赖灰度误差“发现已经偏了以后再补救”。

### 11.6 完成标准

- 入弯无明显甩动；
- 弯中不出现连续左右修正；
- 出弯不冲出外侧；
- 两个半圆表现接近；
- 半圆中速度和差速曲线连续；
- 不因弯道控制导致里程严重漂移。

---

## 12. 阶段 5：路段状态机与精准停车

### 12.1 终点判定原则

最终停车禁止仅使用：

```c
if (distance >= total_track_length)
{
    stop();
}
```

推荐多条件融合：

```text
route_state == FINAL_STRAIGHT
AND
lap_completed == true
AND
odometry in finish_search_window
AND
stop_line_detected_stably
```

### 12.2 启停线检测防误触

启动位置本身就在 A 点附近，因此必须避免刚启动时将启停线误判为完成一圈。

可使用：

- 最小里程门槛；
- 路段状态；
- 已通过 B、C、D 等逻辑状态；
- 启停线连续检测计数；
- 离开起点后的重新武装机制。

### 12.3 停车状态机

```text
RUN
→ APPROACH_FINISH
→ SEARCH_STOP_LINE
→ BRAKE
→ HOLD
```

#### APPROACH_FINISH

- 逐渐降低基础速度；
- 保持循迹能力；
- 避免高速冲过启停线。

#### SEARCH_STOP_LINE

- 里程进入窗口；
- 强化启停线检测；
- 保持低速稳定前进。

#### BRAKE

- 触发制动；
- 可根据实测选择滑行、反向制动或闭环减速；
- 禁止瞬间断电导致不可控滑行。

#### HOLD

- 目标速度置零；
- 清理速度 PI 积分；
- 保持停止状态；
- 显示总时间。

### 12.4 制动补偿

停车位置取决于：

- 检测点到车辆指定测试点的几何偏移；
- 当前速度；
- 电池电压；
- 路面；
- 制动方式；
- 车辆负载。

应记录不同速度下的制动距离，建立补偿表或简单模型。

### 12.5 完成标准

至少连续测试 10 次：

```text
平均停车误差
最大停车误差
标准差
越线次数
提前停车次数
误判次数
```

要求最大绝对误差满足赛题要求，而不只关注平均值。

---

## 13. 阶段 6：代码结构渐进式重构

### 13.1 重构时机

满足以下条件后再进行：

- 控制任务真实周期已测量；
- 里程计公式已验证；
- 主要控制链路已明确；
- 直线和弯道行为已经基本稳定；
- 已有基线测试数据；
- 已有 Git 标签可回退。

### 13.2 推荐分层

```text
Application/
    ChassisTask.c
    RouteManager.c
    TrackingTask.c
    FinishManager.c

Control/
    LineController.c
    SpeedController.c
    HeadingController.c
    CurveFeedforward.c

Estimation/
    EncoderEstimator.c
    Odometry.c
    LineEstimator.c

Hardware/
    Motor.c
    Encoder.c
    Gray.c
    IMU.c
    OledDisplay.c
    StarFlash.c

Platform/
    Scheduler.c
    Timebase.c
    Diagnostics.c
```

应根据现有仓库结构渐进迁移，不要求一次全部改完。

### 13.3 推荐任务划分

#### 高频硬实时任务

周期建议由实际需求确定，例如：

```text
编码器采样
速度估计
速度 PI
电机输出
```

#### 中频控制任务

```text
灰度采样
线位置估计
循迹控制
路段状态更新
```

#### 低频非关键任务

```text
OLED 刷新
星闪状态发送
日志输出
按键状态显示
```

### 13.4 禁止行为

- 在中断中执行 OLED 全屏刷新；
- 在高频任务中阻塞等待串口；
- 高频任务直接 `printf` 大量数据；
- 所有模块共享大量可写全局变量；
- 控制器内部隐式读取硬件；
- 状态机、传感器和电机输出全部写在 `main.c`；
- 一次重构同时改变算法、参数和硬件接口。

### 13.5 推荐接口原则

```text
采样层只产生数据
估计层只处理状态
控制层只计算控制量
执行层只输出到硬件
应用层管理任务和状态机
```

---

## 14. 实验记录格式

每轮实车实验追加到 `docs/EXPERIMENT_LOG.md`，建议格式：

```markdown
## EXP-YYYYMMDD-序号

### 关联问题
- TRACK-001

### Git 信息
- Branch:
- Commit:

### 硬件条件
- 电池电压：
- 车辆负载：
- 路面：
- 轮胎状态：

### 参数
- 基础速度：
- Line P/I/D：
- Left Speed PI：
- Right Speed PI：
- 控制周期：

### 修改内容
- 

### 测试方法
- 

### 原始数据
- 日志文件：
- 视频文件：
- 波形截图：

### 结果
- 最大横向误差：
- 一圈时间：
- 里程读数：
- 实际距离：
- 停车误差：

### 结论
- 

### 下一步
- 
```

---

## 15. 代码修改流程

### 15.1 修改前

```powershell
git status
git branch --show-current
git rev-parse HEAD
```

建议为每个独立问题创建分支：

```powershell
git switch -c fix/track-straight-oscillation
```

### 15.2 修改中

每次只处理一个主要问题，避免把以下内容混在同一提交中：

- 调度结构；
- 控制算法；
- 参数调整；
- 硬件驱动；
- OLED 界面；
- 通信协议。

### 15.3 修改后

```powershell
git add -A
git commit -m "fix: reduce straight-line tracking oscillation"
git push -u origin fix/track-straight-oscillation
```

验证完成后再合并到 `main`。

---

## 16. GPT + GitHub 协作规范

### 16.1 每次分析前提供的上下文

```text
仓库：Jhon0213/bupt-2026-e-car
分支：
Commit：
主问题编号：
现象：
测试条件：
本轮允许修改的范围：
本轮禁止修改的范围：
```

### 16.2 推荐分析提示词

```text
请使用 GitHub 读取 Jhon0213/bupt-2026-e-car 指定分支和提交。

首先阅读：
- docs/CHASSIS_TRACKING_MASTER_PLAN.md
- docs/PROJECT_STATE.md
- docs/ARCHITECTURE_MAP.md
- docs/EXPERIMENT_LOG.md

当前处理问题：
[填写问题编号和现象]

要求：
1. 先定位相关文件、函数和调用链；
2. 分析该问题与任务周期、速度环、里程计和路段状态机的关系；
3. 区分已确认事实、合理推断和待实验验证内容；
4. 先给诊断实验，不直接修改代码；
5. 说明每个实验的预期结果及如何解释；
6. 最后生成本轮状态回写内容。
```

### 16.3 每轮分析结束后的固定输出

```text
【问题编号】
【当前结论】
【证据】
【涉及文件与函数】
【对整体架构的影响】
【对任务周期的影响】
【推荐实验】
【实验预期】
【允许修改的最小范围】
【风险】
【项目状态更新内容】
```

---

## 17. 项目状态更新规则

每完成一个问题，应更新：

1. 本文档对应问题状态；
2. `PROJECT_STATE.md` 当前结论；
3. `EXPERIMENT_LOG.md` 实验数据；
4. `DECISION_LOG.md` 关键方案选择；
5. Git 提交和标签。

禁止仅在聊天窗口中保留重要结论。

---

## 18. Definition of Done

### ARCH 完成标准

- 关键任务周期可观测；
- 无控制任务超周期；
- 非关键任务不阻塞；
- 各模块执行时间有数据。

### ODOM 完成标准

- 每圈脉冲数确认；
- 左右轮距离分别标定；
- 直线误差、弯道误差和整圈误差已分离；
- 里程数据可稳定支持路段判断。

### TRACK 完成标准

- 直线无持续左右振荡；
- 目标速度下不丢线；
- 左右轮速度响应一致；
- 控制输出无高频突变。

### CURVE 完成标准

- 入弯、弯中、出弯连续；
- 两个半圆表现一致；
- 不出现明显外冲和内切；
- 曲线控制不引发显著轮胎侧滑。

### STOP 完成标准

- 不在启动时误判终点；
- 能稳定检测 A 点启停线；
- 连续多次停车最大误差不超过 `2 cm`；
- 停车后无明显回弹或继续爬行。

---

## 19. 当前推荐执行顺序

```text
第 1 步：ARCH-001
测量 10 ms 控制任务的真实周期、最大周期和超周期次数。

第 2 步：ARCH-002 / ARCH-003
测量模块执行时间，排查 OLED、通信、打印和等待操作。

第 3 步：ODOM-001 ~ ODOM-004
确认编码器参数和左右轮独立距离系数。

第 4 步：ODOM-005 ~ ODOM-007
完成直线、半圆和整圈误差分析。

第 5 步：TRACK-001 ~ TRACK-007
处理直线左右摇摆。

第 6 步：CURVE-001 ~ CURVE-006
处理半圆平滑转弯。

第 7 步：STOP-001 ~ STOP-007
实现融合终点检测和精准停车。

第 8 步：ARCH-004 / ARCH-005
在行为和数据明确后，进行渐进式结构重构。
```

---

## 20. 当前下一步

当前建议从 `ARCH-001` 开始，不先修改循迹 PID，不先全面重构。

需要完成：

1. 找到当前 10 ms 任务的触发位置；
2. 找到编码器、灰度、速度 PI、电机输出的实际调用链；
3. 增加最小侵入式周期测量；
4. 记录至少一圈运行中的：
   - 平均周期；
   - 最大周期；
   - 超周期次数；
   - 各主要模块执行时间；
5. 根据测量结果决定是否需要调度重构。

---

## 21. 更新记录

| 日期 | Commit | 更新内容 | 更新人 |
|---|---|---|---|
| 2026-07-31 | 待填写 | 创建底盘、循迹、里程计与实时架构总控文档 | siyuen |

