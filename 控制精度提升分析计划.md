# 小车控制精度提升分析与实施计划

本文档基于当前工程代码的底层阅读整理，目标是作为后续提升小车控制精度的工作大纲。当前没有修改控制代码，只记录问题判断、调试方法、参数重点和算法改进优先级。

## 1. 当前控制链路判断

当前工程实际运行的任务入口在 `main.c`，任务选择为 `TASK_MODE_BONUS_FOUR_LAP`。主循环每次延时约 10ms 后调用任务更新函数。

实际闭环链路大致如下：

```text
main.c
  -> TaskBonusFourLap_Update()
  -> RouteNavigator_Update()
  -> 灰度/IMU 外环控制
  -> 左右轮目标 RPM
  -> 目标速度斜坡限制
  -> SpeedPI 双轮速度 PI
  -> Motor PWM 输出
```

需要注意的是，`Hardware/CONTROL/control.c` 中旧的 `yaw_control_with_gyro()` 虽然存在，但当前主任务链路没有调用它。因此后续改进应优先围绕 `RouteNavigator + SpeedPI + HeadingControl + GrayTrack + Encoder + IMU` 这条实际链路展开，不应直接围绕旧控制文件做主要调参。

## 2. 优先级最高的问题

### P0-1：控制周期和传感器更新时间不够确定

现状：

- 主循环使用前台 `delay_cycles(320000)` 形成约 10ms 周期，但实际周期是“延时 + 执行时间”，不是严格定时。
- 左右编码器速度由两个独立的 10ms 定时器分别更新。
- `RouteNavigator` 内部大量逻辑默认每次更新就是 10ms，例如状态计时 `ms += 10`。
- `HeadingControl` 中积分和微分没有显式 `dt`，微分项是本次误差减上次误差。
- IMU 新数据到达频率不确定，当前只在收到新 yaw 帧时更新航向控制。

影响：

- PID 参数会随实际循环周期、IMU 输出率、串口处理耗时变化。
- 同一组参数在不同电量、不同串口输出率、不同编译优化下可能表现不同。
- 航向微分项不是真正的角速度阻尼，容易出现抖动或滞后。

改进方向：

- 建立统一控制周期，例如由一个固定 10ms 或 5ms 的系统节拍触发控制更新。
- 在控制器内部传入真实 `dt`，至少速度环、航向环、灰度微分都要按 `dt` 计算。
- 对 IMU 数据增加新鲜度判断，例如 yaw 超过 50ms 或 100ms 未更新则进入降级策略，而不是继续使用旧角度。
- 记录真实循环间隔、IMU 帧间隔、编码器速度更新时间，用日志确认周期稳定性。

调试重点：

- 记录每次控制循环的时间戳差值。
- 记录 IMU `frame_count` 增量，确认实际输出率。
- 记录左/右编码器速度更新时间是否同步。
- 在蓝牙或串口日志里增加 `dt_ms`、`imu_age_ms`、`encoder_age_ms`。

### P0-2：IMU 输出率和配置没有被程序完整控制

现状：

- `RobotPlatform.c` 中只把 MCU 端 UART 切到 115200。
- 代码没有看到对 IMU 模块本身发送配置命令，例如设置输出率、设置回传内容、保存配置。
- 本地 IMU 手册显示模块默认输出率可能为 10Hz，并支持 20Hz 到 1000Hz 配置。
- `IMU.c` 解析了 `gyro_z`，但当前实际控制链路主要使用 yaw，未把陀螺仪角速度用于主动阻尼。

影响：

- 如果 IMU 实际只有 10Hz，航向控制每 100ms 才有一次新角度，车速稍高时控制会明显滞后。
- 如果 IMU 之前被外部工具配置过，当前程序启动时无法保证模块处于预期状态。
- 只用 yaw 角做外环时，对快速扰动和转弯动态的抑制不足。

改进方向：

- 启动时主动配置 IMU 输出率，建议先设为 100Hz；如果串口和 MCU 处理裕量足够，再评估 200Hz。
- 启动后读取或统计帧率，确认配置生效。
- 将 `gyro_z` 引入航向控制，形成“角度外环 + 角速度内环”的级联结构。
- 校准 `gyro_z` 的零偏和比例，确认代码里的量程换算与模块实际配置一致。

调试重点：

- 静止时记录 yaw 漂移、gyro_z 零偏和噪声。
- 原地缓慢转 90°，比较 yaw 积分变化与实际角度。
- 统计 5 秒内收到的 IMU 帧数，确认实际 Hz。
- 观察高速直线时 yaw 延迟和 overshoot。

### P0-3：速度环缺少前馈、死区补偿和左右轮差异建模

现状：

- `SpeedPI.c` 使用左右独立增量式 PI，默认参数约为 `Kp = 1.5`、`Ki = 0.06`。
- 左右轮使用同一组 PI 参数。
- 没有 PWM-RPM 前馈模型。
- 没有静摩擦启动补偿、低速死区补偿、电池电压补偿。
- `SpeedPI_BalanceForStraight()` 会把左右 PWM 平均化，这会抹掉左右电机真实差异。
- 已有记录显示左轮可能需要约 PWM 300 才明显启动，而当前速度目标斜坡较慢，低速阶段容易长时间卡在死区内。

影响：

- 低速定位和贴线时容易一边不动、一边动，造成角度误差。
- 只靠积分推过死区，会带来迟滞、超调和重复性差。
- 左右轮机械差异被平均 PWM 掩盖后，短时会出现更大的航向误差。

改进方向：

- 先做左右轮开环 PWM-RPM 标定，得到每个轮子的启动 PWM、停止 PWM、正反向曲线。
- 速度控制改为 `PWM = feedforward(target_rpm) + PI(error)`。
- 对左右轮分别保存前馈系数和死区补偿。
- 加入抗积分饱和，PWM 饱和时限制积分继续累积。
- 直行时不要简单平均左右 PWM，应保留左右轮前馈差异，由航向环修正目标速度差。

调试重点：

- 左右轮分别扫 PWM：上升扫、下降扫、正转、反转。
- 记录 30、60、80、120 RPM 的阶跃响应。
- 观察上升时间、稳态误差、超调、制动后恢复。
- 电池满电和低电压各测一次，确认前馈是否需要电压修正。

### P0-4：四圈任务的圈间切换存在运动状态不连续

现状：

- `RouteNavigator` 结束路线时会调用刹车。
- `TaskBonusFourLap` 几乎立刻启动下一圈，重新初始化控制器。
- 新一圈开始时速度 PI 状态被清零，并进入 coast/重新起步逻辑。

影响：

- 刹车实际作用时间很短，机械上车还在动，但软件认为进入了新的静止起步阶段。
- 积分状态、目标速度、车辆实际速度不连续，容易在圈间产生位置和航向误差。
- 四圈累计误差会明显放大。

改进方向：

- 明确选择一种策略：
  - 连续过圈：不要刹车，不清空关键控制状态，平滑进入下一圈。
  - 定点停稳：刹车后等待速度接近 0，并确认 yaw/位置稳定，再启动下一圈。
- 对比赛任务，如果要求连续跑四圈，优先推荐连续过圈；如果终点动作要求稳定，则终点附近使用速度规划减速，而不是瞬时刹车。

调试重点：

- 记录每一圈切换时的实际 RPM、目标 RPM、PWM、yaw、编码器计数。
- 比较“连续过圈”和“停稳再起步”两种方案的终点偏差方差。
- 重点看第 2、3、4 圈是否误差逐圈变大。

## 3. 第二优先级问题

### P1-1：距离控制目前是阈值触发，不是真正的位置闭环

现状：

- C/A 段间隙通过平均编码器绝对计数判断，例如某段阈值约 10470。
- 到阈值后立即刹车或切换状态。
- 实际停止位置取决于当时速度、电池、地面摩擦、PWM、刹车效果。

影响：

- “触发点”不是“最终位置”。
- 同一个阈值在不同速度下终点会不同。
- 四圈累计时，距离误差会影响下一段入线角度。

改进方向：

- 引入基于剩余距离的速度规划：
  - 远离终点时保持巡航速度。
  - 接近终点时按 `v_allowed = sqrt(2 * a_dec * remaining)` 限速。
  - 终点判定同时满足距离误差小、速度低、持续若干周期。
- 进一步可升级为 trapezoid 或 S-curve 速度曲线。
- 重新标定左右轮脉冲/mm 和有效轮径，不要只依赖理论轮径和编码器 PPR。

调试重点：

- 同一段距离用 40、60、80、100 RPM 重复跑，测实际停点。
- 调不同减速度 `a_dec`，观察终点 overshoot 和时间。
- 记录触发位置、刹车开始位置、最终停止位置三者差异。

### P1-2：航向控制建议升级为角度外环 + 角速度内环

现状：

- `HeadingControl` 当前主要根据 yaw 误差输出左右轮目标 RPM 差。
- 微分项是误差差分，且没有显式 `dt`。
- 没有直接使用陀螺仪 `gyro_z` 做角速度阻尼。

影响：

- 快速偏航时控制只能等 yaw 误差变大后才修正。
- 转向过程容易出现 overshoot。
- 参数对控制频率敏感。

推荐结构：

```text
yaw_target - yaw_measured
  -> 角度外环 P/PI
  -> yaw_rate_target

yaw_rate_target - gyro_z
  -> 角速度内环 P/PI
  -> diff_rpm

base_rpm +/- diff_rpm
  -> 左右轮速度 PI
  -> PWM
```

调参顺序：

1. 先关闭角度积分，只调角速度内环，让小车对旋转扰动有阻尼。
2. 再加角度外环 P，让 yaw 能回到目标角。
3. 只有存在长期静态偏差时，才加入很小的角度积分。
4. 根据车速调度航向增益：速度越高，航向修正上限应更保守，避免蛇形。

### P1-3：灰度巡线目前只做数字量 P 控制，信息利用不足

现状：

- `GrayTrack.c` 能选择靠近上一帧的连续黑线区域，这是当前比较有价值的抗干扰逻辑。
- 控制上主要是数字权重误差，`Kp` 有效，`Kd` 当前基本未使用或很弱。
- “看到黑线”只按任意黑位判断，没有充分区分正常线、宽线、交叉线、丢线、全黑区域。
- 没有基于置信度调整速度。

影响：

- 高速巡线时容易滞后。
- 交叉口或宽黑区域容易误判中心。
- 丢线恢复策略不够明确。

改进方向：

- 先做数字传感器物理位置标定，把每个探头位置映射成 mm。
- 基于连续黑线宽度、位置和历史误差计算置信度。
- 增加滤波后的 D 项，例如对误差低通后求导。
- 根据置信度和误差大小调节基础速度：误差大或置信度低时自动降速。
- 如果硬件支持模拟灰度值，再升级为归一化模拟重心法。

调试重点：

- 静态放在线上，记录每个典型位置的灰度 bit pattern。
- 低速只调 P，让车能稳定回线。
- 加 D 后看是否减少摆动，若噪声变大则先滤波再加 D。
- 分别测试直线、缓弯、急弯、十字、丢线恢复。

## 4. 建议的调试路线

### 阶段 1：先建立评价指标

不要先盲目调 PID。需要先定义精度指标，否则每次试车只能靠感觉判断。

建议记录：

- 直线 1m 的终点横向误差、航向误差。
- 固定路线每圈终点误差的平均值和标准差。
- 速度阶跃响应：上升时间、稳态误差、超调。
- 航向保持误差：yaw RMS、最大偏差、回正时间。
- 巡线误差：灰度 error RMS、丢线次数、最大连续丢线时间。
- 四圈任务：每圈入口误差、出口误差、总完成时间、是否逐圈漂移。

每次改参数后至少重复 5 次，同一套数据看平均值和方差。控制精度提升的核心不是单次跑得好，而是重复性变好。

### 阶段 2：底层传感器和执行器标定

先标定这些基础量：

- 左轮 PWM-RPM 曲线。
- 右轮 PWM-RPM 曲线。
- 左右轮启动 PWM、停止 PWM。
- 正反转是否对称。
- 编码器脉冲/mm。
- 实际轮距或等效转向系数。
- IMU yaw 静态漂移。
- IMU `gyro_z` 零偏、噪声、比例。
- IMU 实际输出率。

推荐测试：

- 架空轮子做 PWM 扫描，得到基础曲线。
- 落地低速运行，检查静摩擦和地面负载下曲线变化。
- 固定 PWM 或固定 RPM 跑直线，测左右里程差。
- 原地转 90°、180°、360°，用编码器和 IMU 分别估算角度，比较误差。

### 阶段 3：先把速度环做稳

速度环是最底层，必须先稳定。否则航向和巡线都在补偿电机误差，参数会互相污染。

推荐顺序：

1. 关闭复杂外环，只给左右轮相同目标 RPM。
2. 加左右轮独立前馈，让目标速度附近不用靠积分硬推。
3. 设置 `Ki = 0`，先调 `Kp`，让响应快但不过度振荡。
4. 加很小 `Ki` 消除稳态误差。
5. 加积分限幅和饱和保护。
6. 测低速 20-40 RPM，重点解决死区和爬行。
7. 测中速 60-120 RPM，重点解决超调和左右一致性。

重点参数：

- `SpeedPI` 的 `Kp`、`Ki`。
- PWM 上限和下限。
- 左右轮前馈斜率。
- 左右轮启动补偿 PWM。
- 目标速度斜坡 `RPM/tick`。
- 刹车和 coast 切换策略。

### 阶段 4：再调航向

速度环稳定后，再调航向才有效。

推荐顺序：

1. 先用 IMU yaw 做低速直线保持。
2. 引入 `gyro_z` 做角速度阻尼。
3. 建立角度外环 + 角速度内环。
4. 对 `diff_rpm` 做限幅，防止高速时修正过猛。
5. 做速度增益调度。

重点参数：

- 航向角外环 `Kp_angle`。
- 航向角外环 `Ki_angle`，初期建议为 0。
- 角速度内环 `Kp_rate`。
- 角速度内环 `Ki_rate`，初期建议为 0 或很小。
- `diff_rpm` 最大限制。
- yaw 误差死区。
- IMU 数据超时阈值。

### 阶段 5：再调灰度巡线

巡线不是只调一个 `Kp`。它依赖速度环、航向控制和传感器识别质量。

推荐顺序：

1. 固定低速，验证灰度 error 的符号和大小。
2. 调 P，让车能稳定回到线中心。
3. 对 error 做滤波后加入 D，降低摆动。
4. 加线型识别和置信度。
5. 根据误差和置信度调整基础速度。
6. 处理丢线和交叉口逻辑。

重点参数：

- 灰度传感器物理位置权重。
- `gray_kp`。
- `gray_kd`。
- error 低通滤波系数。
- 最大差速限制。
- 巡线基础速度。
- 丢线搜索速度和方向保持时间。
- 宽线/交叉线识别阈值。

### 阶段 6：最后优化路线状态机和四圈稳定性

当底层稳定后，再处理路线状态机。

推荐顺序：

1. 单独跑每一段，确认段内误差。
2. 加速度规划，使每段终点速度可控。
3. 明确圈间策略：连续过圈或停稳过圈。
4. 记录每圈误差，找累计漂移来源。
5. 对关键状态切换增加确认条件，例如位置 + 速度 + 传感器模式持续若干周期。

重点参数：

- 每段目标距离。
- 减速开始距离。
- 最大巡航速度。
- 最大加速度和减速度。
- 终点速度阈值。
- 终点确认周期数。
- 圈间等待时间或连续过圈保持策略。

## 5. 新算法和改进优先级

下面是建议优先级，越靠前越应该先做。

| 优先级 | 算法/改进 | 原因 | 风险 |
| --- | --- | --- | --- |
| 1 | 左右轮独立前馈 + PI + 死区补偿 | 直接解决电机不一致、低速卡滞和积分迟滞，是所有外环的基础 | 需要实测标定 |
| 2 | 统一控制周期 + 显式 dt + 传感器新鲜度判断 | 让 PID 参数具有可重复性，避免 IMU 低频或旧数据污染控制 | 需要整理调度逻辑 |
| 3 | 基于剩余距离的梯形/S 曲线速度规划 | 减少终点 overshoot，让路线切换位置更稳定 | 需要标定减速度 |
| 4 | yaw 角度外环 + gyro_z 角速度内环 | 比单纯 yaw PID 更适合动态航向控制，可减少摆动和超调 | 需要确认 gyro_z 标定 |
| 5 | 灰度误差滤波 PD + 置信度 + 速度调度 | 提升高速巡线稳定性，改善丢线和交叉口处理 | 依赖传感器模式测试 |
| 6 | 编码器 + IMU 互补融合估计姿态 | 可抑制 IMU 漂移和编码器打滑误差 | 算法复杂度中等 |
| 7 | Pure Pursuit 或 Stanley 路径跟踪 | 如果路线几何明确，可从状态机控制升级为路径跟踪 | 需要明确地图和定位 |
| 8 | EKF 融合定位 | 在高精度路线和多传感器融合时有价值 | 开发和调参成本高 |
| 9 | MPC、自适应 PID、模糊控制 | 理论上可提升性能，但当前底层问题未解决前收益不稳定 | 不建议早期投入 |

短期最有效的三件事：

1. 统一控制节拍、记录真实 `dt`、确认 IMU 输出率。
2. 做左右轮 PWM-RPM 标定，引入前馈和死区补偿。
3. 引入 `gyro_z` 角速度环，并用剩余距离速度规划替代阈值后急刹。

## 6. 建议日志字段

当前已有路线日志有一定基础，但对 100Hz 控制瞬态不够。建议增加或调整如下字段：

```text
timestamp_ms
dt_ms
state
route_lap
left_count
right_count
left_delta
right_delta
left_rpm
right_rpm
target_left_rpm
target_right_rpm
base_rpm
diff_rpm
left_pwm
right_pwm
speed_error_l
speed_error_r
speed_pi_p_l
speed_pi_i_l
speed_pi_p_r
speed_pi_i_r
yaw
yaw_target
yaw_error
gyro_z
imu_age_ms
gray_bits
gray_error
gray_confidence
remaining_distance
event
battery_voltage
```

如果蓝牙实时发送带宽不足，建议先在 RAM 中缓存关键数据，停车后再分批发送。调参阶段的数据完整性比实时显示更重要。

另外，`tools/bt_logger.py` 当前更像是为 8 字段开环数据设计的；如果继续用 `RouteNavigator` 的详细日志，需要同步更新解析脚本字段格式，否则日志保存和后处理会不匹配。

## 7. 关键文件定位

以下文件是后续改进的主要入口：

- `main.c`：任务选择、主循环节拍。
- `Application/TaskBonusFourLap.c`：四圈任务和圈间切换。
- `Application/RouteNavigator.c`：路线状态机、IMU/灰度控制融合、速度目标生成、状态切换。
- `Hardware/CONTROL/SpeedPI.c`：左右轮速度 PI、PWM 输出和直行平衡逻辑。
- `Hardware/CONTROL/HeadingControl.c`：航向控制器。
- `Hardware/CONTROL/GrayTrack.c`：灰度巡线误差和差速生成。
- `Hardware/Encoder.c`：编码器计数和 RPM 计算。
- `Hardware/IMU.c`：IMU 数据解析、yaw/gyro_z 更新。
- `Application/RobotPlatform.c`：平台初始化、IMU 串口配置。
- `tools/bt_logger.py`：上位机日志采集脚本。

## 8. 不建议优先做的事

以下方向不是完全没用，但不适合现在作为第一步：

- 直接大幅调整航向 PID，而不先确认控制周期、IMU 输出率和速度环稳定性。
- 直接启用旧的 `yaw_control_with_gyro()`，因为它绕过当前速度环并直接输出 PWM，和现有架构不一致。
- 一开始就上 EKF、MPC、自适应控制等复杂算法。当前主要瓶颈更可能在周期、执行器建模、速度环和路线切换。
- 只看单次成功结果，不做重复实验和方差统计。
- 只调一个全局速度或一个 `Kp`，这会掩盖左右轮差异、摩擦死区和传感器延迟。

## 9. 推荐实施顺序

建议按下面顺序推进：

1. 增强日志和真实周期测量。
2. 确认并固定 IMU 输出率。
3. 左右轮 PWM-RPM 标定。
4. 速度环加入前馈、死区补偿、抗积分饱和。
5. 独立调好左右轮速度阶跃响应。
6. 航向控制引入 `gyro_z` 角速度内环。
7. 灰度巡线加入滤波 D、置信度和速度调度。
8. 距离段加入减速规划。
9. 处理四圈任务的圈间连续性。
10. 重复跑完整路线，按数据定位剩余误差来源。

这套顺序的核心原则是：先让底层速度可控，再让航向可控，然后再谈巡线和路线状态机。否则后面的算法会一直在补偿底层不确定性，调出来的参数很难复用。
